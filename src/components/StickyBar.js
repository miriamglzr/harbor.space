import React from 'react';
import stickyBar from '../media/stickybar.svg';

export default function StickyBar () {
  return (
    <div>
      <img className="stickyBar" src={stickyBar} alt="stickybar" />
    </div>
  );
}
